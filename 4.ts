// 4. Написати функцію, яка приймає два параметра. Перший параметр - число, яке має підноситись до степеня вказаного в другому параметрі. Функція вертає результат піднесення до степеня.

let x = Math.pow(2, 3);
console.log(x);

//
let y = 2 ** 2;
console.log(y);
